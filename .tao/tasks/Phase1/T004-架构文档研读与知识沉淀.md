# T004: 架构文档研读与知识沉淀（coralnpu 微架构）

## 执行环境
**执行环境**：本地

## 接口规范
- 输入：coralnpu `doc/`（overview.md、integration_guide.md、microarch/{microarch,dispatch,lsu,mlu,debug}.md、tutorials/writing_coralnpu_programs.md）；T002 提供的可运行环境（便于对照）
- 输出：`.tao/knowledge/coralnpu-architecture.md` 架构知识笔记（主仓库侧）
- 约束：不写实现代码；笔记须基于文档与 RTL 源码（hdl/chisel/src/），区分"事实"与"推断"

## 验收标准
1. 笔记覆盖以下主题，每项引用具体文档/源码路径；**对微架构描述采用"核实引导"而非断言**（文档与 RTL 不一致处须并列记录差异）：
   - 总体架构：scalar + vector(RVV SIMD) + matrix 三类处理组件的数据流与协作
   - ISA：以 `doc/microarch/microarch.md` 与 README 为准记录支持的扩展；**核实 README（`rv32imf_zve32x_zicsr_zifencei_zbb`）与 toolchain（`toolchain/cc_toolchain_config.bzl` 的 `rv32imf_zve32f_..._zfbfmin_zvfbfmin_zvfbfwma`）的 ISA 配置差异并记录差异**
   - 存储层次：ITCM(8KB)/DTCM(32KB) TCM、L1 icache/dcache、AXI master/slave 接口（doc/integration_guide.md 中的信号语义）
   - 微架构：**按 `doc/microarch/microarch.md` 核实流水线级数（官方文档为 three-stage pipeline），不预设阶段数**；核实 dispatch/retire 机制表述；**核实 RTL 中的 RetirementBuffer（`hdl/chisel/src/coralnpu/RetirementBuffer.scala`）并在笔记中说明它与文档 retired 机制的对应/差异**
   - matrix/vector 单元（mlu.md、lsu.md）的关键结构
2. 与 T002/T003 运行结果对照，说明软件程序（ELF）如何被加载与执行（如 .ld 链接脚本、TCM 布局）
3. 笔记被 reviewer 抽查 3 处引用均可在仓库中找到

## 完成区
**状态**：已验证（reviewer 第 1 轮 Accepted + Mimo 交叉复核确认）
**Commit**：无（只读研读 + 新增知识笔记 `.tao/knowledge/coralnpu-architecture.md`，未改 coralnpu/ 内任何文件；提交由主会话验收后统一处理）
**测试结果**：无需构建/测试（只读研读任务）。验证手段 = 真实工具链命令（readelf/objdump 解析 T003 产物 ELF）验证笔记中 ELF 段布局/ISA 属性/反汇编引用，全部命中。
**修改文件**：新增 `.tao/knowledge/coralnpu-architecture.md`（304 行，8 章）；更新本任务文件完成区
**验收结果**：
- 笔记覆盖 6 项验收标准（总体架构/ISA 三处差异/存储层次/微架构/matrix-vector 单元/ELF 加载对照），每项引用具体文档或源码路径
- 关键引用抽查（全部命中）：
  - `README.md` L22 `rv32imf_zve32x_zicsr_zifencei_zbb`；L24 "Four-stage processor, in-order dispatch, out-of-order retire"
  - `toolchain/cc_toolchain_config.bzl` L164 `rv32imf_zve32f_zicsr_zifencei_zbb_zfbfmin_zvfbfmin_zvfbfwma`
  - `toolchain/build_scripts/coralnpu_v2_toolchain_build.sh` L49 multilib `rv32imf_zicsr_zifencei_zbb-ilp32--`
  - `doc/microarch/microarch.md` L7 "in-order three-stage pipeline"
  - `hdl/chisel/src/coralnpu/RetirementBuffer.scala` L50-51 按序出队注释；L517-528 deqReady 逻辑
  - `Parameters.scala` L106 `useRetirementBuffer = enableVerification`；`SCore.scala` L64 mini 模式
  - 实测：T003 ELF readelf 段布局（.text@0x0/.data@0x10000/Entry 0x0）与 .ld 链接脚本一致；ELF attributes 证实实际编译 ISA 为 zve32f+bf16 扩展
**新发现/坑**：
1. **README 与 RTL 冲突（重要）**：README 声称 "out-of-order retire"，但 RetirementBuffer.scala 明确按序出队（in-order retire）；"Four-stage" 与 microarch.md "three-stage" 冲突。已并列记录。
2. **ROB 默认不启用**：`useRetirementBuffer = enableVerification`，core_mini_axi_sim 默认 mini 模式（仅 nRetired 计数）；完整 ROB 仅 core_mini_verification_axi_cc_library 启用。Parameters.scala 注释披露：RVV 禁用+Float 启用配置下完整 ROB 有 hang bug。
3. **L1 cache 未在 CoreAxi 实例化**：L1ICache/L1DCache 仅定义接口（供 Chipyard/TLUL），core_mini_axi 用 UncachedFetch。
4. **ISA 三处不一致**：README(zve32x) / cc_toolchain_config.bzl(zve32f+bf16，实际生效) / multilib(无向量)。
5. 向量寄存器文档 v0..v63 256 位 vs RTL rvvRegCount=32、rvvVlen=128。
**遗留问题**：无（笔记已覆盖全部验收标准；流水线 stage 数因文档冲突无定论，按任务要求并列记录）

## 审阅记录

#### 第 1 轮 engineer 自审
（自主自审·嵌套受限——任务文件明确"不要嵌套调子代理，自主执行与自审"，故按全局规则降级为自主自审，三要素审查：逻辑正确性/设计惯用法/防造假）

审查对象：`.tao/knowledge/coralnpu-architecture.md` 全文（8 章）+ 完成区。

**意见与问题**：
1. 逻辑正确性：§6.1 ELF 段地址（.text@0x0、.crt@0x260、.data@0x10000、.bss@0x10070、.heap@0x100c0、.stack@0x17f80、Entry 0x0）与 readelf 原始输出逐项核对，一致；§6.2 CSR 事务（0x30000/0x30004/0x30008）与 `core_mini_axi_tb.cc` L271/273（csr_addr_=0x30000）核对一致。
2. 逻辑正确性：§4.4 对 "out-of-order retire" 的差异结论（RTL 按序出队）有源码证据（L50-51 注释 + L517-528 Ctz(blockRetire)），并标注 README 可能指执行乱序，属推断——符合任务"核实引导而非断言"要求。
3. 设计/惯用法：笔记将「事实」与「推断」显式分区标注（共 5 处推断标注），引用全部采用仓库相对路径+行号，符合 reviewer 抽查要求。
4. 防造假：所有实测数据（readelf/objdump/ELF attributes）来自真实工具链命令输出；8 处关键引用行号逐一 `sed` 复验命中。
5. 边界情况：检查 ISA 是否含 A 扩展——README/multilib 均无 'a'，Decode.scala 无原子指令解码字段，笔记未断言含 A，正确。

**finding 处置**：

| finding | 处置 | 改了什么 | 复验证据 |
|---------|------|---------|---------|
| 教程文档（writing_coralnpu_programs.md）加载流程未成节覆盖 | ✅已修 | 新增 §6.3（cocotb 教程视角：load_elf/lookup_symbol/execute_from/wait_for_halted + .data 段约定） | 与 elf.cc::LookupSymbol、core_mini_axi_tb.cc 加载流程交叉一致 |
| §1.2 向量寄存器 256 位描述可能被误读为 RTL 事实 | ✅已修 | 撰写时即在正文括号标注 RTL rvvVlen=128/rvvRegCount=32，差异汇总表 §8 单列 | Parameters.scala L98/L122 |
| 实测数据防造假核查 | ✅已修 | 无改动（核查通过） | readelf/objdump 真实输出 + 8 处行号 sed 复验 |
| A 扩展边界情况 | ✅已修 | 无改动（核查通过：无 A 扩展声明与解码） | README/multilib 无 'a'；Decode.scala 无原子指令字段 |

**判决**：通过，可提交验收。所有 finding 已修或核查通过，无遗留未修项。

#### 第 1 轮 reviewer 验收
（审查者独立验证的重跑记录、约束核验、判决；Needs Revision 返工后，下一轮标 `第 2 轮`）

**审查对象**：`.tao/knowledge/coralnpu-architecture.md`（304 行，8 章）+ 任务文件完成区。只读研读任务，无构建/运行。

**重跑记录**（独立执行，日志留存 `.tao/logs/T004-review-readelf-objdump.log`）：

1. **引用抽查**（任务要求 ≥3 处，实际核验 40+ 处，全部命中）：
   - `README.md` L12-13 三组件 / L22 `rv32imf_zve32x_zicsr_zifencei_zbb` / L24 "Four-stage processor, in-order dispatch, out-of-order retire" / L29 SRAM —— `sed -n` 逐行命中 ✅
   - `toolchain/cc_toolchain_config.bzl` L164 `rv32imf_zve32f_zicsr_zifencei_zbb_zfbfmin_zvfbfmin_zvfbfwma`（is_rv64=False 分支）✅；build_scripts L49 multilib `rv32imf_zicsr_zifencei_zbb-ilp32--;rv64imf_zicsr_zifencei_zbb-lp64--`、L58 llvmorg-20.1.0 ✅
   - `doc/microarch/microarch.md` L7 "in-order three-stage pipeline" + 三阶段描述 + L23-30 延迟表 ✅
   - `hdl/chisel/src/coralnpu/RetirementBuffer.scala` L44-52 生命周期注释、L50-51 "dequeued...in-order"、L517-528 `blockRetire`/`Ctz(blockRetire)` deqReady、L336-361 vectorReady 匹配 ✅
   - `Parameters.scala` L44-48 MemoryRegions（ITCM 0x0/8K、DTCM 0x10000/32K、Peripheral 0x30000）、L49-55 highmem、L75 instructionLanes=4、L82-87 ROB hang bug 注释、L98 rvvVlen=128、L106 `useRetirementBuffer=enableVerification`、L121 rvvRegfileBaseAddr=64、L122-125 retirementBufferSize=16/retirementLanes=8/rvvRegCount=32、L162-174 L1 参数 ✅
   - `SCore.scala` L54-65 实例化顺序、L56-57 `enableFetchL0=False→UncachedFetch`、L64 `mini=!useRetirementBuffer`、L96-99 4×ALU/4×BRU/1×MLU/1×DVU、L190 nRetired、L599-603 `debug.rb`/RvviTrace ✅
   - `CoreAxi.scala` L105-115 ClockGate、L150-165 TCM 3 端口仲裁、L243-253 AxiSlave→FabricMux、L260-282 AXI 读写仲裁（id=0 ebus/id=1 ibus 回路由）✅
   - `integration_guide.md` L21-31 信号表、L36-74 m_axi 语义、L80-91 s_axi 语义、L99 resp 0/OKAY|2/SLVERR、L119-151 Debug Signals、L159-161 ITCM/DTCM/CSR 地址表、L164-165 同步复位、L167-218 Booting（PC_START 0x30004→时钟门控→复位→轮询 STATUS 0x30008）✅
   - `CoreAxiCSR.scala` L31-163 CoreCSR、L50 `resetReg=RegInit(3.U)`、L51-52 pcStartReg 首拍捕获 boot_addr、L102-103 地址映射 ✅
   - `tests/verilator_sim/elf.cc` L20-36 LoadElf（按 p_paddr 复制 PT_LOAD）、L38-90 LookupSymbol ✅；`core_mini_axi_sim.cc` L66-81 加载流程 ✅；`core_mini_axi_tb.cc` L316-390 ELF 写后回读校验、L357-358 写 CSR 0x30004=entry、L363-380 tohost 查找、L381-387 非 ELF 裸写；`core_mini_axi_tb.h` L273 `csr_addr_=0x30000` ✅
   - `coralnpu_tcm.ld.tpl` L5-10 MEMORY（ITCM/DTCM/EXTMEM 0x20000000/DDR 0x80000000）✅；`linker.bzl` L26-47 heap/stack 余量逻辑、非默认尺寸 DTCM 基址 0x100000 ✅
   - matrix/vector：`rvv_backend_mac_unit.sv` L60-72 mac8 输入与 `mac8_out` VLEN/2 tile（"4*4 elements. Total: VLEN/2"）✅；`RvvCore.sv` L15-16 N=4/CMD_BUFFER_MAX_CAPACITY=16、L104-110 RvvFrontEnd 实例化 ✅；`mlu.md` L8-11/L46-55 三段流水 ✅；`lsu.md` L11-13 slot ✅；`dispatch.md` L7-10/L13-17/L19-28/L30-33/L35-41 ✅；`overview.md` L14-23/L34-44/L47-60/L62-71/L73-90 ✅；`Decode.scala` L316-320 DispatchV2/L338-341 分支交错/L344-381 标量 scoreboard/L384-412 浮点 scoreboard ✅；`Fetch.scala` L15-17 分支假定/L108-118 Predecode ✅；`Lsu.scala` L66-67 LsuV3/L449 LsuSlot/L1479 LsuSuperSlot/L3246-3260 区域判定/L3298-3342 ebus size 收窄 ✅；`RvvCore.scala` L705-791 RvvCoreShim ✅；`flags.bzl` L17-25 `--enableRvv=True` ✅；`BUILD` L557-600 配置矩阵、L710-726 rvv 变体 ✅；`coralnpu_v2.bzl` L339-387 DENYLIST ✅；`writing_coralnpu_programs.md` L30-38/L44-57/L104-112/L125-130/L154-160/L186-190/L210-214 ✅；`debug.md` L96-101 AXI CSR 0x30800-0x30814、L109-113/L154 data0/dmcontrol/dmstatus/abstractcs/command、L181-182 regno 0x1000-0x101F/0x1020-0x103F ✅
   - **44 个笔记引用路径批量存在性检查：全部 OK** ✅

2. **readelf/objdump 独立重跑**（工具链 `~/.cache/bazel/_bazel_gxt/09e84813c895531dea3658f72a6bca69/external/toolchain_coralnpu_v2/bin/`；产物 `coralnpu/bazel-out/k8-fastbuild-ST-dd8dc713f32d/bin/examples/coralnpu_v2_hello_world_add_floats.elf`，验证其确实存在）：
   - `readelf -A`：`Tag_RISCV_arch: "rv32i2p1_m2p0_f2p2_zicsr2p0_zifencei2p0_zmmul1p0_zfbfmin1p0_zfhmin1p0_zbb1p0_zve32f1p0_zve32x1p0_zvfbfmin1p0_zvfbfwma1p0_zvl32b1p0"` —— 与笔记 §2.2 **逐字符一致** ✅（实测确认实际编译 ISA = zve32f+bf16 扩展）
   - `readelf -h`：ELF32 / RISC-V / Entry point 0x0 ✅
   - `readelf -S`：`.text` Addr 0x0、`.crt` 0x260、`.data` 0x10000、`.bss` 0x10070、`.heap` 0x100c0、`.stack` 0x17f80 —— 与笔记 §6.1 **全部一致** ✅
   - `readelf -l`：两个 LOAD `0x0 R E FileSiz 0x2d0` 与 `0x10000 RW MemSiz 0x8000`、Entry 0x0 ✅
   - `objdump -d <main>@0x144`：flw/fadd.s/fsw 循环体 PC 0x158-0x174、input1@0x10000/input2@0x10020/output@0x10040 —— 与笔记 §6.1 一致 ✅
   - 生成 `.ld`：ITCM ORIGIN=0x0 LENGTH=8K、DTCM ORIGIN=0x10000 LENGTH=32K、EXTMEM 0x20000000/4096K、DDR 0x80000000/2048M；`__global_pointer$` L91-92、`_ret` L102 ✅

3. **附加核查**（任务要求 1-2 处，实际 3 处）：
   - **RetirementBuffer in-order retire**：`rg` + 读源码确认 L50-51 "dequeued from the buffer in-order" + L517-528 `blockRetire`（lane0 需 valid+cfDone，lane>0 还需前一条无 trap）→ `deqReady=Ctz(blockRetire)`。README "out-of-order retire" 与 RTL 的差异成立 ✅
   - **L1 cache 未实例化**：`rg -n "L1ICache|L1DCache" hdl/chisel/src/` 仅命中 Parameters.scala（注释）、BUILD（源文件列表/Emit app）、L1ICache.scala/L1DCache.scala 自身；Core.scala/CoreAxi.scala/CoreTlul.scala 无实例化。`enableFetchL0=False→UncachedFetch`（SCore.scala L56-57）✅
   - **ISA 实际生效配置**：readelf -A 实测（见上）✅
   - **coralnpu/ 零改动**：`git -C coralnpu status --short` 无输出（干净）；主仓库仅新增笔记 + 修改任务文件 ✅

**约束核验**：
- 不写实现代码 ✅（产出仅为 .md 知识笔记）
- 笔记基于文档与 RTL 源码 ✅（44 处引用路径全部真实存在）
- 区分「事实」与「推断」✅（§1.2/§2.3/§4.1/§4.4/§5.2 等多处显式标注推断）
- 微架构差异"并列记录"而非断言 ✅（流水线 three-stage vs four-stage、retire 序、ISA 三处、向量寄存器 256 vs 128 均并列）
- 与 T002/T003 对照 ✅（ELF 段布局/加载流程/CSR 事务均以 T003 真实产物 + sim 源码独立复验）

**发现的小瑕疵（不影响验收标准，建议后续可选修正）**：
1. §3.3 将 `rb` 列为 integration_guide.md debug 信号（引用范围 L119-151），但该文档 Debug Signals 表无 `rb`；`debug.rb` 是 RTL 侧端口（SCore.scala L599 `debug.rb := robDebug`）。属 RTL 事实混入文档引用，论断正确但归源略宽。
2. §4.1 引 dispatch.md L17 "All execution units read their operands from the register file the cycle after..."，实际该句在 L15-16，L17 为 "WAR hazard never occurs."。内容真实存在，行号偏差 1-2 行。

**判决**：**Accepted**。验收标准 1-4 与附加核查全部通过：笔记覆盖 6 项主题且引用具体路径；三处 ISA 差异并列、流水线冲突并列、RetirementBuffer 对应/差异均有据；抽查引用（40+ 处，含 44 个路径存在性）全部命中；readelf/objdump 独立重跑与笔记实测数据逐字符一致；关键论断（in-order retire、L1 未实例化、ISA 生效配置）源码/工具独立验证成立；事实与推断区分清楚；coralnpu/ 零改动。两处轻微瑕疵不影响任务验收标准，标注供架构师终审参考。

#### 第 1 轮 architect 交叉复核（Mimo）

**复核结论**：**确认 Accepted**，通过收尾。

- 验收标准 3 条全覆盖且超额（reviewer 抽查 40+ 引用 vs 要求 3 处）；笔记 8 章覆盖全部主题
- reviewer 判决合理，无过严/过松；关键论断（RetirementBuffer in-order retire、L1 cache 未实例化、ISA 生效配置）经源码/readelf 独立验证准确，事实/推断区分清楚
- 2 处轻微瑕疵（§3.3 `rb` 归源、§4.1 dispatch.md 行号）属实但**不影响验收标准**（论断正确仅归源/行号微调）；已由主会话修正
- 建议：通过收尾

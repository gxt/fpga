# T005: 构建链路与验证体系梳理（bazel target 图谱）

## 执行环境
**执行环境**：机器201

## 接口规范
- 输入：coralnpu 构建文件（WORKSPACE、MODULE.bazel、.bazelrc、rules/{chisel,verilog,coco_tb,verilator,sram_backdoor}.bzl、hdl/chisel/src/coralnpu/BUILD、tests/{cocotb,verilator_sim}/BUILD、toolchain/）；T003 完成后的实际构建产物对照
- 输出：`.tao/knowledge/coralnpu-build-map.md` 构建链路笔记（主仓库侧）
- 约束：不写实现代码；须结合实际跑过的构建命令验证理解

## 验收标准
1. 笔记清晰描述两条链路并含关键 BUILD target 名称与依赖关系：
   - RTL 生成链：Chisel Scala → `chisel_binary` 生成 SystemVerilog（如 `//hdl/chisel/src/coralnpu:core_mini_axi_cc_library` 产出 `CoreMiniAxi.sv`，`//hdl/chisel/src/coralnpu:rvv_core_mini_axi_cc_library` 产出 `RvvCoreMiniAxi.sv`）→ verilator_cc_library 模型 → C++ tb → 可执行 sim
   - 软件链：`coralnpu_v2_binary`（交叉编译 RISC-V）→ ELF（含 ITCM/DTCM 布局）→ 加载到 sim/FPGA
2. 列出 Cocotb、Verilator C++ sim、UVM（仅说明不执行）三套验证的入口 target 与运行命令
3. 说明 hermetic verilator、RISC-V 工具链、第三方依赖（opentitan/libsystemctlm-soc 等）从哪拉取、如何缓存
4. 用 `bazel query`（或等价）对至少 3 个关键 target 输出依赖关系作为证据，**query 命令与原始输出须原文留存到笔记**（附日期与 commit 号）
5. 笔记被 reviewer 抽查关键命令可复现

## 完成区
**状态**：已验证（reviewer 第 1 轮 Accepted + Mimo 交叉复核确认）
**Commit**：无（本任务为知识梳理，零代码改动；笔记与日志均为工作产物，尚未提交）
**测试结果**：通过 6/6 项验收标准（见下）；bazel query/cquery 6 个 target 全部 exit 0；build 3 条缓存命中全部成功
**修改文件**：
- 新建 `.tao/knowledge/coralnpu-build-map.md`（构建链路与验证体系笔记）
- 留存日志 `.tao/logs/T005-*.log` × 12（gitignore 忽略，不进 git）
- 本任务文件（完成区更新）
**验收结果**：
1. RTL 生成链 + 软件链：笔记 §1/§2 完整描述，含关键 BUILD target 与依赖关系（chisel.bzl 展开的 5 子 target、core_mini_axi_cc_library 的 verilator_cc_library 属性、软件链 ELF/BIN/VMEM 产出）
2. 三套验证入口：笔记 §4（Cocotb `bazel test //tests/cocotb:...`；Verilator C++ sim `bazel build //tests/verilator_sim:core_mini_axi_sim --linkopt=-latomic` + 运行；UVM 仅说明 Makefile 流程）
3. 依赖拉取与缓存：笔记 §3（hermetic verilator b97df914 源码构建、RISC-V 工具链 GCS tar.xz、opentitan/libsystemctlm-soc/SystemC/Chisel-Maven 等来源 + 缓存位置）
4. bazel query 证据：**6 个 target** 实测（core_mini_axi_cc_library、_emit_verilog、core_mini_axi_sim、cocotb meta、hello_world、rvv_core_mini_axi_cc_library + 2 个 deps + 1 个 cquery），命令与原始输出留存日志，笔记 §5 附日期（2026-08-17）与 commit（coralnpu d93b5550）
5. 命令可复现：笔记 §5.7 列 build 验证命令，§6 常用命令速查；日志可供 reviewer 抽查
6. 约束核验：未写任何实现代码；所有理解基于真实跑过的 query/build 命令
**新发现/坑**：
- `core_mini_axi_cc_library` 名字带 `cc_library` 但实为 SystemC `verilator_cc_library`，**纯 SV 导出应看 `_emit_verilog` genrule**（产出 .sv/.h/.zip）；此命名易误导，笔记 §1.1 已专门说明
- `bazel query` 静态、不展开配置；`bazel cquery` 配置化（输出带配置哈希，如 `(4a7dfb7)`）。查定义用 query、查配置展开用 cquery，二者不可混用
- `deps(x, 2)` 含大量样板依赖（absl/bazel_tools/remotejdk），展示需过滤但原始日志必须全量留存
- CoreMiniAxi.sv 实际由 CIRCT firtool-1.114.0 生成（首行标注），Maven jar 版本 1.114.0 与编译器内部版本 `1.114.0-2-g6cf2492e1` 差 2 commits
- 建议沉淀：`chisel_cc_library` 宏的 5 子 target 映射关系（emit_verilog/verilog/SystemC 模型/纯 C++ 模型）是理解整个 RTL 生成链的钥匙，已写入笔记 §1.1
**遗留问题**：
- UVM 体系仅"说明不执行"（机器201无 VCS/license），`//tests/uvm:uvm_sim_verilator` bazel target 未实际构建验证（README 声明运行需 VCS 环境）
- 笔记中 cocotb meta target（20 case）未全量跑（T002 仅跑过 csr_test 单测），全量回归不在本任务范围

## 审阅记录

#### 第 1 轮 engineer 自审
（自主自审 · 嵌套受限，按 rule-engineer 自审流程降级执行）

**自审范围**：笔记 `.tao/knowledge/coralnpu-build-map.md`（无实现代码，逐行核对事实）。

**审查项与判决**：

1. **逻辑正确性**（逐项核对源码 + 真实输出）
   - `chisel_cc_library` 5 子 target 映射：对照 `rules/chisel.bzl` L143-203 逐行核对，emit_verilog/verilog/SystemC/纯 C++ 描述准确 ✅
   - `core_mini_axi_cc_library` 属性：query 输出 `systemc=True`、`module="..._verilog"`、`module_top="CoreMiniAxi"`，笔记转述准确 ✅
   - 生成 flags：对照 BUILD L565-574（core_mini_axi）、L716-721（rvv）+ flags.bzl，逐项一致 ✅
   - `CoreMiniAxi.sv` 证据：实际文件 39835 行、首行 firtool 标注、`module CoreMiniAxi` 位于 L22255，笔记数字与实测一致 ✅
   - 软件链：coralnpu_v2.bzl 宏产出 elf/bin/vmem 三 filegroup（L317-336）、linker script 生成（L276-303）、GCS 工具链 URL（WORKSPACE L272-274）均对照源码 ✅
   - 三套验证入口：BUILD 行号与 target 名逐一核对（tests/verilator_sim L219-266、tests/cocotb L183-219、tests/uvm L84-111）✅
   - 依赖来源：verilator b97df914（缓存 external/rules_hdl/dependency_support/verilator/verilator.bzl L25 实测）、libsystemctlm-soc 79d624f3、opentitan 0e3cf622、SystemC 2.3.4 均对照 repos.bzl/deps.bzl ✅
2. **设计/惯用法**：笔记采用"总览 → 两链 → 三验证 → 依赖 → 证据 → 速查 → 坑"结构，与既有 toolchain-notes.md / coralnpu-architecture.md 的"事实/推断"风格一致；query 证据原文留存日志文件而非只写摘要 ✅
3. **防造假**：所有 query/cquery/build 均真实执行（exit 0）；12 个日志文件在 `.tao/logs/T005-*.log` 可复查；笔记中缓存命中数（1151/14/2688）来自真实 build 输出 ✅

**Findings 处置表**：

| finding | 处置 | 改了什么 | 复验证据 |
|---------|------|---------|---------|
| F1: deps 输出含样板依赖，笔记展示过滤行可能让 reviewer 误以为漏项 | ✅已修 | 笔记 §5.3 注明"原始完整日志保留在 .tao/logs/T005-deps-core_mini_axi_sim-2.log"（日志为 tee 全量输出，含 absl/bazel_tools 等） | 实际 `cat .tao/logs/T005-deps-core_mini_axi_sim-2.log` 可见全量 67 行 |
| F2: 笔记需注明 UVM 未执行范围 | ✅已修 | 笔记 §4.3 明确"仅说明不执行，机器201无 VCS/license"；完成区"遗留问题"同步标注 | 与任务验收标准 2"UVM（仅说明不执行）"一致 |
| F3: cocotb meta target 全量 case 未跑 | ⏸延后 | 非本任务范围（T002 已跑 csr_test 单测留证）；完成区"遗留问题"记录 | 任务接口规范未要求执行测试，仅要求列出入口与命令 |

**自审判决**：通过。全部 finding 已处置（F1/F2 已修，F3 延后并在遗留问题标注）。笔记 6 项验收标准逐条满足，可提交 reviewer 独立验收。
**（自主自审 · 嵌套受限）**

#### 第 1 轮 reviewer 验收
（审查者独立验证的重跑记录、约束核验、判决；Needs Revision 返工后，下一轮标 `第 2 轮`）

#### 第 1 轮 reviewer 验收

**审查来源**：独立代码审查（review 命令，build agent 独立核验，非采信完成区）。

**重跑/核验记录**（逐项对照源码、构建文件与日志）：
- 6 项验收标准对应事实抽查 11 项全部一致：chisel_cc_library 展开 5 子 target（rules/chisel.bzl L150-203）、core_mini_axi_cc_library systemc=True + genrule 产物（BUILD L557-577）、rvv/highmem/512KB/verification 变体 gen_flags（BUILD L601-760 + flags.bzl）、Cocotb meta 20 case（BUILD L119-183）、UVM uvm_sim_verilator（BUILD L84-111）、coralnpu_v2.bzl 链接流程、工具链 -march/-mabi、依赖来源（GCS/rules_hdl/llvm-firtool/opentitan 等 sha256 pin）、CoreMiniAxi.sv 39835 行 + firtool 1.114.0 + module L22255、build 缓存命中数与 cquery 配置哈希、HEAD d93b5550
- 命令可复现性（验收 5）：关键 build/query 命令原始日志全部留存，产物文件真实存在

**发现的问题（小修订，不影响验收）**：5 条行号/表述精度问题，已由主会话修正：
1. §1.2 rvv 行行号锚点（BUILD L710-723 → 注明宏调用点 L690）
2. §3 verilator 行"coralnpu 挂 19 个 patch"→ 明确 19 个 patch 打在 rules_hdl、verilator 自身 1 个 patch
3. §3 MODULE.bazel 行号 L49-50 → L83-84
4. §4.3 命名 coralnpu_cosim_lib_static → coralnpu_cosim_lib_static_archive（BUILD L79-82）
5. 任务文件自审 F1 "68 行" → "67 行"

**判决**：**Accepted**（无事实性错误；UVM 未执行、cocotb 全量未跑已在遗留问题如实标注，符合验收标准"仅说明不执行"）

#### 第 1 轮 architect 交叉复核（Mimo）

**复核结论**：**确认 Accepted**，通过收尾。

- 验收标准 5 条全覆盖：两条链路（reviewer 独立验证 chisel.bzl 5 子 target、coralnpu_v2.bzl transition）、三套验证（UVM 标注未执行符合要求）、依赖来源 sha256 pin 逐项核对、query 证据 6 target 超要求、命令可复现
- 关键论断独立抽查 3 处全部准确：`core_mini_axi_cc_library` 实为 SystemC verilator 模型（systemc=True，命名易误导已在笔记 §7 标注）、`_emit_verilog` 纯 SV 导出 genrule（CoreMiniAxi.sv 39835 行/firtool 1.114.0/module L22255 全中）、软件链 platform transition 机制
- reviewer 判决合理，无过严/过松；5 条行号精度修订已在笔记生效
- 约束遵守：coralnpu 零改动、笔记基于真实命令输出

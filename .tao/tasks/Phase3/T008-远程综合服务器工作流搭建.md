# T008: 机器202工作流搭建

## 执行环境
**执行环境**：机器202（`gxt@192.168.200.202`）＋ 机器201

## 接口规范
- 输入：机器202已确认（`gxt@192.168.200.202`，Vivado v2025.1 与机器201一致，登记于 `.tao/knowledge/registry.md`；文件交换方式待 T008 阶段确认，scp 候选）；coralnpu 源码；bazel 生成的 RTL 产物（`//hdl/chisel/src/coralnpu:core_mini_axi_cc_library` 等）
- 输出：主仓库 `synth/` 下的工作流脚本（同步 coralnpu、RTL 产物、工程目录）；**机器202执行拓扑记录（`.tao/knowledge/synth-server.md`）**
- 约束：机器201不承担综合主责；机器202访问信息登记到 `.tao/knowledge/registry.md` 机器路由表；不把密码/密钥写入任务文件或脚本；**执行模式已定：路径 A（机器201 ssh 直连机器202执行，不在机器202部署 opencode），拓扑记录见 `.tao/knowledge/synth-server.md`**

## 验收标准
1. 机器202信息（地址、Vivado 版本、文件交换方式）已确认并登记到 registry.md
2. 同步脚本可将 coralnpu 源码与 bazel 生成的 RTL（如 `//hdl/chisel/src/coralnpu:core_mini_axi_cc_library`）推送到机器202并可拉回结果
3. 机器202执行 `vivado -version`（或等效）输出已记录，确认版本 ≥ 项目要求且支持目标器件
4. **确定机器202执行拓扑并写入 `.tao/knowledge/synth-server.md`，至少覆盖三要素**：
   - ① 机器202跑什么：`bazel fusesoc_build`（机器202上完整 bazel 流程）还是同步工程后纯 Vivado（机器201生成/推送工程，机器202只跑 synthesis/implementation）
   - ② 依赖到位方式：机器201 bazel 产物推送 / 机器202自拉缓存（含 RISC-V 工具链、IP 等第三方依赖的放置与版本确认）
   - ③ 每步执行机器：明确综合、实现、生成 bitstream 每一步在机器201还是机器202执行
5. **license 检查**：确认机器202 Vivado 能识别目标器件（`xc7v2000tflg1925-1`，及可选的 xcvu13p 基线器件），记录 license 检查方式与结果（全功能 license 已确认，此处仅验证识别能力）

## 完成区
**状态**：已验证（reviewer 第 2 轮 Accepted + Mimo 交叉复核确认）
**Commit**：无（未提交；engineer 不主动 commit，主会话收尾时提交）
**测试结果**：全部通过——功能路径 6/6（info / push src / push rtl core_mini_axi / push rtl rvv_core_mini_axi / push synth / pull / exec），错误路径 2/2 正确拒绝（push 无参数、push rtl 未知 key 均 exit 1 并报错），语法检查 bash -n 通过
**修改文件**：
- 新建 `synth/sync.sh`（同步工作流脚本，从 registry.md 解析机器202地址/Vivado 路径）
- 新建 `synth/README.md`（用法 + 机器202目录布局）
- 新建 `synth/.gitignore`（忽略 out/、日志产物）
- 修改 `.tao/knowledge/registry.md`（登记文件交换方式 + 机器202工具事实）
- 修改 `.tao/knowledge/synth-server.md`（拓扑三要素决策 + vivado-version/license 关键命令记录）
- 修改本任务文件（完成区 + 自审记录）
**验收结果**：
1. 机器202信息登记 ✅ registry.md 已有地址/版本；文件交换方式已确认登记：**标准入口 `synth/sync.sh`，rsync 增量为主 + scp 单文件，机器202工作根 `~/fpga/`**（本次已实测）
2. 工作流脚本 ✅ 实测：`push src` 推送 coralnpu 源码 1269 文件 19.5MB；`push rtl core_mini_axi` 推送 CoreMiniAxi.sv(1.8M)/VCoreMiniAxi_parameters.h/CoreMiniAxi.zip；`push rtl rvv_core_mini_axi` 推送 RvvCoreMiniAxi.sv(23.5M)；机器202 find 验证齐全；`pull` 拉回机器202 work/ 模拟结果 report.txt+top.bit 到机器201 synth/out/
3. vivado -version ✅ 机器202实测 `vivado v2025.1 (64-bit) / SW Build 6140274`，与机器201一致 ≥ 项目要求；日志 `.tao/logs/T008-vivado-version.log`，机器202留存 `~/fpga/T008-vivado-version.txt`
4. 拓扑三要素 ✅ 已写入 synth-server.md（见下节摘要）：① 机器202跑纯 Vivado（synth/impl/bitstream）；② 依赖=机器201 bazel 产物推送；③ RTL 生成机器201、综合/实现/bitstream 机器202、结果拉回机器201
5. license 检查 ✅ 机器202 `vivado -mode batch` + `get_parts`：`xc7v2000tflg1925-1: RECOGNIZED`、`xcvu13p-fhga2104-2-e: RECOGNIZED`、virtex7 全族 203 part；日志 `.tao/logs/T008-license-get_parts.log`，tcl 留存机器202 `~/fpga/T008-get_parts.tcl`
**新发现/坑**：
- 机器202实测**无 bazel/bazelisk/fusesoc、无 ~/.cache/bazel**（`which` 为空）→ 直接支撑拓扑①决策（机器202不跑完整 bazel 流程）
- 机器202 CPU 为 Xeon E5620（2010 年 2.4GHz 老架构）——综合耗时预期明显高于现代 CPU，T009 长综合要设充足 timeout
- 机器202 Python 3.12.3（比机器201新），若 T009 需在机器202跑任何 Python 脚本注意版本差异
- rsync 增量交换高效（源码 19.5MB 首次全量、后续增量）；bazel 产物路径固定 `bazel-out/k8-fastbuild/bin/hdl/chisel/src/coralnpu/`（脚本 RTL_OUT_SRC 依赖此路径，bazel-out 与 bazel-bin 符号链接不同，勿用 bazel-bin）
- registry.md 的机器路由表 push 命令格式仍是 `scp <file> ...`，本次文件交换实际以 rsync 增量为主、scp 单文件为辅，两者并存（push/pull 均已实测）
**遗留问题**：
- 未 commit（engineer 不主动提交，主会话 `/complete` 收尾时提交）
- 机器202 `~/fpga/work/synth_t008_check/`（report.txt/top.bit）与机器201 `synth/out/synth_t008_check/` 为**验证用模拟数据**，非真实综合结果；真实综合在 T009/T010 执行
- 若 T009 官方器件（xcvu13p）需走 fusesoc 完整流程，需评估在机器202安装 bazelisk + 冷拉依赖（增强项，当前拓扑不启用，已记录 synth-server.md）
- 自审 F3（`sync.sh` 无参数时 usage 输出含文件头/映射表注释，较冗余）：❌不修，需精简时再改

## 拓扑三要素决策摘要（详细记录见 synth-server.md）

| 要素 | 决策 |
| --- | --- |
| ① 机器202跑什么 | **同步工程后纯 Vivado**：机器202不跑 bazel/fusesoc（实测无该工具），只接受机器201推送的 RTL 与工程，跑 `vivado -mode batch`（synth/impl/write_bitstream） |
| ② 依赖到位方式 | **机器201 bazel 产物推送**：RTL 机器201 `bazel build ..._emit_verilog` 生成 SV，`sync.sh push rtl <key>` 推送机器202 `~/fpga/rtl_out/<key>/`；机器202不自拉 bazel 缓存（第三方依赖不需要到机器202） |
| ③ 每步执行机器 | RTL 生成（Chisel→SV）：**机器201** bazel；综合/实现/bitstream：**机器202** Vivado batch（机器201 ssh 直连托管）；结果拉回**机器201**分析 |

## 审阅记录

#### 第 1 轮 engineer 自审
（自主自审·嵌套受限：用户指定本任务不嵌套子代理，按 rule-engineer 自审三要素逐项审查）

**审查范围**：`synth/sync.sh`（重点）、`synth/README.md`、`registry.md`、`synth-server.md` 改动；所有命令真实执行，日志留存 `.tao/logs/T008-*.log`（remote-probe / vivado-version / license-get_parts / sync-info / push-src / push-rtl / pull）。

| # | 意见/问题 | 判决 | 处置 |
|---|----------|------|------|
| F1 | `info` 硬编码 Vivado 路径 `/tools/Xilinx/2025.1/...`，若机器202路径变化需改脚本，与"主机地址只写 registry"原则不一致 | 采纳 | ✅已修：改为从 registry.md 解析 `grep -o '/tools/Xilinx/[0-9.]*/Vivado/bin/vivado'`，保留 `SYNTH_VIVADO` 环境变量覆盖与默认兜底 |
| F2 | `main` 未统一检查 SERVER 解析失败（registry 无匹配时后续命令会以空 user 报 ssh 错，信息不明确） | 采纳 | ✅已修：`main` 入口统一 `[[ -n "$SERVER" ]] || die ...`，所有子命令共享 |
| F3 | `usage`（无参数）输出含文件头注释与映射表注释，较冗余 | 保留 | ❌不修：纯展示问题，不影响任何功能；`usage` 本就是辅助信息，精简优先级低（已在遗留问题登记） |
| F4 | `push rtl` 中 `src_files=(${RTL_FILES[$name]})` 依赖 word splitting，文件名含空格会破坏 | 保留 | ❌不修：产物文件名固定无空格（CoreMiniAxi.sv 等），bazel 命名约定保证；加空格反而复杂化 |
| F5 | 脚本依赖 bash 4.3+ 关联数组 `-v` 检查 | 保留 | ❌不修：环境实测 bash 5.1.16，无兼容性问题 |
| F6 | 防造假确认：各验收点输出均来自真实命令（ssh/rsync/scp/vivado batch），机器202文件用 find/ls 实际验证，pull 拉回文件内容用 find 核对 | 判决通过 | ✅已确认（无改动） |

**自审结论**：逻辑正确性、设计/惯用法、防造假三要素通过；两处改进（F1/F2）已修并复验（`./sync.sh info` 正常输出解析后的 Vivado 路径；`bash -n` 通过）。所有 finding 已处置，无未修阻断项，状态可标「待验收」。

#### 第 1 轮 reviewer 验收
（审查者独立验证；判决：**Needs Revision**）

**重跑记录（均为 reviewer 独立执行，输出留存 `.tao/logs/T008-review-*.log`）**：

| # | 验收命令 | 结果 | 说明 |
|---|---------|------|------|
| R1 | `ssh gxt@192.168.200.202 "/tools/Xilinx/2025.1/Vivado/bin/vivado -version"` | exit=0 ✅ | `vivado v2025.1 (64-bit)`、SW Build 6140274，与机器201一致 |
| R2 | 独立 tcl（`/tmp/review-get_parts.tcl`）机器202 `vivado -mode batch` + `get_parts` | exit=0 ✅ | `xc7v2000tflg1925-1: RECOGNIZED`、`xcvu13p-fhga2104-2-e: RECOGNIZED`、virtex7 全族 203 part（日志 `T008-review-license-get_parts.log`） |
| R3 | `bash synth/sync.sh info` | exit=0 ✅ | 正确解析 `gxt@192.168.200.202`、`/tools/Xilinx/2025.1/Vivado/bin/vivado`、`~/fpga`，机器202返回 `zzx-NF5280` + v2025.1 |
| R4 | `bash synth/sync.sh push src` | exit=0 ✅ | rsync 增量 1269 文件 19.5MB；二次推送 transferred=0（增量有效） |
| R5 | `bash synth/sync.sh push rtl core_mini_axi` | **exit=1 ❌** | **scp: Permission denied**（见下） |
| R6 | `bash synth/sync.sh push rtl rvv_core_mini_axi` | **exit=1 ❌** | 同上 |
| R7 | `bash synth/sync.sh push synth` | exit=0 ✅ | 机器202 `~/fpga/synth/` 三文件齐全 |
| R8 | `bash synth/sync.sh pull synth_t008_check` | exit=0 ✅ | 机器202 work/ 拉回 `synth/out/synth_t008_check/report.txt`(15B)+`top.bit`(13B) |
| R9 | `bash synth/sync.sh exec "pwd && ..."` | exit=0 ✅ | 机器202执行 `cd ~/fpga` 生效，EXEC_OK |
| R10 | 错误路径：`push`（无参）/`push rtl`（无 key）/`push rtl unknown_key` | exit=1 ✅×3 | 中文错误提示明确 |
| R11 | `bash -n synth/sync.sh` | exit=0 ✅ | 语法通过 |
| R12 | `git -C /home/gxt/fpga status --short` | ✅ | **coralnpu/ 零改动**（空输出）；`synth/` 未跟踪新增；改动仅 registry.md、synth-server.md、任务文件 |
| R13 | 机器202探针（hostname/nproc/mem/disk/which bazel） | ✅ | 与任务完成区一致：zzx-NF5280、16核 Xeon E5620、62GiB、`/` 691G；无 bazel/bazelisk/fusesoc |

**关键缺陷（R5/R6 根因，已独立复现）**：
- bazel 产物源文件为只读（`-r-xr-xr-x`，0444），scp 复制后机器202文件也是只读；
- 机器202**已存在**同名文件时，`scp -o BatchMode=yes ...` 无法覆盖只读目标 → `scp: Permission denied`，exit=1；
- 即 **`push rtl` 只在首次（机器202无文件）成功，重复推送必然失败**——这是 RTL 产物更新后重新同步的正常使用场景，违反验收标准 2；
- 复现验证：手动 `chmod 444` 的机器202文件被 scp 覆盖 → Permission denied（exit=1）；换 rsync 同场景 → exit=0 且内容更新。
- 工程师完成区"push rtl 推送成功"仅覆盖首次推送场景，自审/测试未覆盖重复推送。

**约束核验**：
1. 机器202信息登记 registry.md（地址/版本/交换方式）✅
2. 同步脚本 push coralnpu 源码+RTL、pull 结果 —— **push rtl 重复推送失败 ❌**
3. 机器202 vivado -version 记录、版本一致 ✅
4. synth-server.md 拓扑三要素齐全（①纯 Vivado ②机器201产物推送 ③RTL机器201/综合实现机器202/结果拉回）✅，且有实测依据（机器202无 bazel）
5. license 检查 RECOGNIZED ✅
- 无密码/密钥硬编码（脚本依赖 BatchMode 免密，registry 仅存地址）✅
- 机器202地址只在 registry.md，脚本 grep 解析 ✅（`gxt@[0-9.]*` 与 `/tools/Xilinx/...` 均能正确解析）

**判决：Needs Revision**（验收标准 2 核心路径失败）。

**具体整改项（唯一阻断项）**：`synth/sync.sh` 的 `cmd_push_rtl` 将 scp 改为 rsync（已验证可覆盖只读机器202文件、重复推送成功），或推送前先删除机器202同名只读文件（如 `ssh_run "rm -f $REMOTE_ROOT/rtl_out/$name/*"`）。改后需复验：连续两次 `bash synth/sync.sh push rtl core_mini_axi` 均 exit=0，且机器202文件内容为最新。

**采信说明**：R13 机器202探针与完成区中 bazel 缺失、CPU/内存/磁盘数据一致，但该项为环境事实且不影响判决；其余全部为 reviewer 独立重跑。

#### 第 2 轮 reviewer 验收
（审查者独立验证返工修复；判决：**Accepted**）

**背景**：第 1 轮唯一阻断缺陷 = `cmd_push_rtl` 用 scp 推送只读（0444）产物，机器202已存在时无法覆盖只读目标 → 重复推送 `scp: Permission denied`。本轮复验修复。

**重跑记录（均为 reviewer 独立执行，日志留存 `.tao/logs/T008-review2-*.log`）**：

| # | 验收命令 | 结果 | 说明 |
|---|---------|------|------|
| R2-1 | 读 `synth/sync.sh` `cmd_push_rtl` | ✅ | 第 82-84 行已改 `rsync -a -e "ssh -o BatchMode=yes"` 推送（不再用 scp） |
| R2-2 | `bash -n synth/sync.sh` | exit=0 ✅ | 语法通过 |
| R2-3 | `bash synth/sync.sh push rtl core_mini_axi`（第 1 次） | exit=0 ✅ | 机器202 rtl_out/core_mini_axi/ 三文件已存在且为只读 0444（正是 scp 失败场景） |
| R2-4 | `bash synth/sync.sh push rtl core_mini_axi`（第 2 次，连续） | exit=0 ✅ | **重复推送成功，第 1 轮缺陷闭环** |
| R2-5 | `bash synth/sync.sh push rtl rvv_core_mini_axi` | exit=0 ✅ | RvvCoreMiniAxi.sv 推送成功 |
| R2-6 | md5 校验机器202 vs 机器201 | 4/4 MATCH ✅ | CoreMiniAxi.sv / VCoreMiniAxi_parameters.h / CoreMiniAxi.zip / RvvCoreMiniAxi.sv 内容与机器201完全一致 |
| R2-7 | 独立覆盖测试：机器202文件改坏（22B `CORRUPTED-BY-REVIEWER`，md5 `2cef31bc...`）并 `chmod 444` 后 `push rtl core_mini_axi` | exit=0 ✅ | 推送后机器202 md5 恢复为机器201 `c21c6a15...` —— **rsync 确能覆盖只读机器202文件并写入最新内容** |
| R2-8 | `ssh ... vivado -version` | exit=0 ✅ | `vivado v2025.1 (64-bit)`、SW Build 6140274，与机器201一致 |
| R2-9 | 独立 tcl（`/tmp/review2-get_parts.tcl`）机器202 `vivado -mode batch` + `get_parts` | exit=0 ✅ | `xc7v2000tflg1925-1: RECOGNIZED`、`xcvu13p-fhga2104-2-e: RECOGNIZED`、virtex7 全族 203 part |
| R2-10 | 错误路径：`push`（无参）/`push rtl`（无 key）/`push rtl unknown_key` | exit=1 ✅×3 | 中文错误提示明确，修复未破坏错误路径 |
| R2-11 | `bash synth/sync.sh info` | exit=0 ✅ | 正确解析 `gxt@192.168.200.202`、`/tools/Xilinx/2025.1/Vivado/bin/vivado`，机器202返回 `zzx-NF5280` + v2025.1 |
| R2-12 | `git -C /home/gxt/fpga status --short` | ✅ | **coralnpu/ 零改动**（不在变更列表）；改动仅 registry.md、synth-server.md、任务文件 + `synth/` 未跟踪新增 |

**约束核验（逐条）**：
1. 机器202信息登记 registry.md（地址 `gxt@192.168.200.202`、Vivado v2025.1 Build 6140274、文件交换方式=标准入口 `synth/sync.sh`，rsync 增量为主+scp 单文件）✅
2. 同步脚本 push coralnpu 源码 + RTL、pull 结果 —— **push rtl 重复推送现 exit=0（R2-3/4/5），验收标准 2 恢复满足** ✅
3. 机器202 vivado -version 记录、版本与机器201一致（R2-8）✅
4. synth-server.md 拓扑三要素齐全（①纯 Vivado ②机器201 bazel 产物推送 ③RTL 机器201生成/综合实现机器202/结果拉回机器201），有实测依据（机器202无 bazel）✅
5. license 检查两器件 RECOGNIZED、virtex7 203 part（R2-9）✅
- 无密码/密钥硬编码（脚本依赖 BatchMode 免密，registry 仅存地址）✅
- 机器202地址只在 registry.md，脚本 grep 解析 ✅

**结论**：第 1 轮唯一阻断缺陷已闭环——`cmd_push_rtl` scp→rsync 修复有效，独立复验连续两次 `push rtl core_mini_axi` 均 exit=0，且覆盖只读机器202文件（内容被破坏并 chmod 444 后推送恢复）验证通过；其余验收项抽查无回归，无新增问题。判决 **Accepted**（仍需主会话更新任务状态为「已验证」）。

**审阅边界说明**：本轮重点为复验第 1 轮返工修复 + 抽查原验收点（R2-8/9/10/11/12），其余第 1 轮已验项（push src / push synth / pull / exec / 机器202探针）未重复执行，采信第 1 轮记录。

#### 第 1 轮 architect 交叉复核（Mimo）

**复核结论**：**确认 Accepted**，通过收尾。

- 代码独立审查：sync.sh 结构清晰、错误处理完备、无密码/密钥硬编码；rsync push rtl 修复正确（bazel 产物 555 只读实测属实，rsync 原子替换可覆盖）；registry.md 解析机制稳定
- 验收标准 5 条全覆盖；reviewer 两轮判决合理（第 1 轮 scp 覆盖只读缺陷真实关键、第 2 轮修复验证超最低要求含覆盖测试+md5 校验）
- 拓扑三要素与 ADR-002 一致，决策有实测依据（机器202无 bazel）
- 约束无违反：coralnpu 零改动、路径 A
- 唯一非阻断说明：registry.md 第 7 行传输命令 scp→rsync 一致性（已由主会话收尾修正）
